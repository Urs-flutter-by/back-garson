## 🎯 Цель
Провести комплексный анализ кода проекта, написанного на **Dart (Frog framework)**, на предмет:
- соответствия современным стандартам и лучшим практикам языка Dart,
- архитектурной чистоты (Clean Architecture),
- проблем с производительностью и утечками ресурсов,
- причин ошибки **"Too many connections"** при запуске приложения.

---

## 🧩 Структура проекта (требует уточнений)
Проект построен по принципам **чистой архитектуры**, и включает следующие слои:
lib/
├── application/
│ └── services/
├── data/
│ ├── sources/
│ ├── repositories/
│ └── models/
├── domain/
│ ├── entities/
│ └── usecases/
├── presentation/
│ ├── pages/
│ ├── widgets/
│ └── controllers/
├── utils/
routes/
├── app/
├── web/

- **`/routes/app` и `/routes/web`** — два отдельных интерфейса (API endpoints) для разных клиентов, работающих с одной базой данных.
- **`/lib/application`** — слой бизнес-логики и сервисов.
- **`/lib/data`** — реализация репозиториев, подключений к БД и моделей.
- **`/lib/domain`** — слой сущностей и бизнес-правил.
- **`/lib/presentation`** — пользовательский слой (интерфейс, контроллеры, виджеты).
- **`/lib/utils`** — вспомогательные функции и константы.

---

## 🧠 Задачи для Gemini

### 1. 🔍 Анализ кода
Провести проверку всех модулей (`lib/`, `routes/`) на:
- Соответствие современным стандартам Dart 3+.
- Использование `final`, `const`, `sealed`, `extension`, `late`, `async/await`, `typedef`.
- Оптимизацию структуры слоёв в соответствии с Clean Architecture.
- Проверку циклических зависимостей.
- Возможные ошибки при работе с потоками, Future и async API.

---

### 2. ⚙️ Проверка зависимостей
- Проанализировать `pubspec.yaml`.
- Проверить все пакеты на наличие **актуальных мажорных версий**.
- Составить таблицу с рекомендациями по обновлению (с указанием возможных breaking changes).
- Предложить оптимизацию зависимостей (удаление неиспользуемых пакетов).

---

### 3. 🧩 Анализ проблем с подключениями ("Too many connections")
Проверить исходный код в `lib/data/` и `application/services` на:
- Множественные инициализации соединений с БД.
- Отсутствие пулов соединений (`connection pool`).
- Неосвобождённые соединения после запросов (`connection.close()` или `await db.release()`).
- Потенциальные async-утечки при старте двух приложений (`routes/app` и `routes/web`), если они создают свои отдельные коннекты к одной БД.
- Рекомендовать оптимальный способ управления соединениями (например, singleton-пул или lazy initialization).

---

### 4. 📚 Генерация API-документации
Извлечь и задокументировать все маршруты (`routes/app`, `routes/web`):
- Сгруппировать по функциональным областям.
- Для каждого маршрута определить:
    - HTTP-метод (GET, POST, PUT, DELETE),
    - путь (endpoint),
    - параметры запроса (query/path),
    - тело запроса,
    - тело ответа,
    - возможные статусы и ошибки.


---

### 5. 📈 Оптимизация и рекомендации
- Составить отчёт по производительности и возможным точкам роста.
- Выявить дублирование кода, неиспользуемые импорты, мёртвый код.
- Проверить все слои на соблюдение принципа единой ответственности (SRP).

---

## 🧾 Формат отчёта
ИИ должен сгенерировать:
1. **`/log/analysis_report.md`** — детальный отчёт по коду.
2. **`/log/dependencies_update.md`** — список обновляемых пакетов и версий.
3. **`/log/api_documentation.md`** — сгенерированная документация по CRUD методам.
4. **`/log/connection_issue_analysis.md`** — причины и рекомендации по устранению “Too many connections”.

---

## 🧠 Инструкции для Gemini CLI
- Игнорировать: `.dart_tool/`, `.idea/`, `build/`, `test/`.
- Использовать стабильную версию Dart SDK ≥ 3.4.
- Фокус анализа: `lib/`, `routes/`, `pubspec.yaml`.
- Все отчёты сохранять в `/log/`.
- Документацию форматировать в Markdown.
- При обновлении зависимостей использовать `pub outdated` и `pub upgrade --major-versions`.

---

## ✅ Ожидаемый результат
После выполнения анализа ИИ должен предоставить:
1. Полный отчёт о качестве и стиле кода.
2. Рекомендации по устранению архитектурных нарушений.
3. Таблицу обновлений зависимостей.
4. Документацию по всем CRUD API.
5. Диагностику и рекомендации по устранению ошибки **“Too many connections”**.

---

## 📅 История изменений / LOG
| Дата | Изменения |
|------|------------|
| {{current_date}} | Первичная инициализация GEMINI.md для анализа проекта |
| — | Добавлена секция проверки подключения к БД |
| — | Добавлены логи анализа и документации |

---

## 🔬 РЕЗУЛЬТАТЫ АНАЛИЗА (05.10.2025)

**Анализ проекта завершен. Все цели, поставленные в задачах, были выполнены.**

Ниже приведена краткая сводка по результатам. Детальные отчеты находятся в директории `/log`.

### 🌟 Что хорошо (The Good)
- **Архитектура:** Проект в целом следует принципам Чистой Архитектуры с правильным разделением на слои.
- **Код:** Код хорошо отформатирован, а именование сущностей понятное.

### ⚠️ Что плохо (The Bad)
- **Безопасность:** Пароль от БД хранится в открытом виде, а для хэширования паролей официантов используется устаревший `md5`.
- **Производительность:** В коде обнаружена классическая проблема **"N+1 запросов"**, которая может сильно замедлять получение данных.
- **Нарушение архитектуры:** Обнаружены импорты слоя `data` внутри `domain`, что нарушает правило зависимостей.

### ❌ Что ужасно (The Ugly)
- **Утечка соединений с БД:** Найдена **критическая ошибка**, из-за которой на каждый HTTP-запрос создается новое подключение к базе данных, которое никогда не закрывается. Это является основной причиной ошибки **"Too many connections"**.

---

### 📂 Сгенерированные отчеты

Все детальные отчеты находятся в директории `/log`:

1.  **[Анализ зависимостей](./log/dependencies_update.md):** Таблица с устаревшими пакетами и рекомендации по их обновлению.
2.  **[Анализ проблемы "Too many connections"](./log/connection_issue_analysis.md):** Подробное описание причины утечки соединений и пошаговое руководство по исправлению.
3.  **[Детальный анализ кода](./log/analysis_report.md):** Полный разбор архитектуры, качества кода и рекомендации по улучшению.
4.  **[Документация по API](./log/api_documentation.md):** Автоматически сгенерированный список всех эндпоинтов проекта.

**Рекомендация:** Начать исправление с проблемы "Too many connections", так как она является блокирующей.

---

### 🛠️ РЕЗУЛЬТАТЫ РЕФАКТОРИНГА (05.10.2025)

#### Часть 1: Исправление утечки соединений
- **Что сделано:** Проведен полный рефакторинг системы подключения к БД. Внедрен пул соединений, обновлены все репозитории и маршруты.
- **Отчет:** **[Сводка рефакторинга (Часть 1)](./log/refactoring_summary_part1.md)**

#### Часть 2: Исправление предупреждений анализатора
- **Что сделано:** Устранены все ошибки и предупреждения (`errors`, `warnings`), выявленные `dart analyze` после первого этапа рефакторинга. Добавлена зависимость `crypto`, исправлена типизация `Pool`, устранены проблемы в моделях данных и маршрутах.
- **Отчет:** **[Сводка рефакторинга (Часть 2)](./log/refactoring_summary_part2.md)**
