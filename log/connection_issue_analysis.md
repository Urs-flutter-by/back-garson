# 🔌 Анализ проблемы "Too many connections"

## 🎯 Диагноз

**Проблема найдена.** Основная причина ошибки — создание нового подключения к базе данных на **каждый** входящий HTTP-запрос, без закрытия предыдущих соединений.

Это происходит в файле `routes/_middleware.dart`.

### 🔥 Проблемный код (`routes/_middleware.dart`)

```dart
Handler middleware(Handler handler) {
  return (context) async {
    // !!! ПРОБЛЕМА !!!
    // Эта строка выполняется при каждом запросе, открывая новое соединение.
    final db = DatabaseSource(); 

    // ... все сервисы создаются с этим новым соединением ...

    // ... старое соединение никогда не закрывается (db.close() не вызывается) ...
    
    final response = await handler(updatedContext);
    return response;
  };
}
```

Каждый вызов `DatabaseSource()` открывает новое "висящее" соединение. Через несколько десятков или сотен запросов сервер PostgreSQL достигает своего лимита на количество одновременных подключений и начинает отказывать в новых.

## 🛠 Рекомендации по исправлению

Необходимо перейти от создания одиночных соединений к использованию **пула соединений (Connection Pool)** и сделать его **глобальным (Singleton)** для всего приложения. Пул будет эффективно управлять ограниченным числом "живых" соединений и переиспользовать их для всех запросов.

### Шаг 1: Модифицировать `lib/data/sources/database.dart` для использования пула

Вместо `Connection` будем использовать `Pool` из пакета `postgres`.

```dart
// lib/data/sources/database.dart

import 'package:back_garson/utils/config.dart';
import 'package:postgres/postgres.dart';

/// Глобальный класс для управления пулом соединений с базой данных.
/// Использует паттерн Singleton, чтобы экземпляр был один на все приложение.
class DatabaseSource {
  // Приватный конструктор
  DatabaseSource._();

  // Статический экземпляр класса (Singleton)
  static final DatabaseSource instance = DatabaseSource._();

  late final Pool _pool;
  bool _isInitialized = false;

  /// Инициализирует пул соединений.
  /// Этот метод нужно вызвать один раз при старте сервера.
  void initialize() {
    if (_isInitialized) return;

    final endpoint = Endpoint(
      host: Config.dbHost,
      port: Config.dbPort,
      database: Config.dbName,
      username: Config.dbUser,
      password: Config.dbPassword,
    );

    _pool = Pool.withEndpoints(
      [endpoint],
      settings: const PoolSettings(
        // Установите максимальное количество одновременных соединений
        maxConnectionCount: 10, 
      ),
    );
    _isInitialized = true;
  }

  /// Предоставляет доступ к пулу соединений.
  Pool get pool {
    if (!_isInitialized) {
      throw StateError('DatabaseSource не инициализирован. Вызовите initialize() при старте приложения.');
    }
    return _pool;
  }

  /// Закрывает все соединения в пуле.
  /// Этот метод нужно вызвать при graceful shutdown сервера.
  Future<void> close() async {
    if (_isInitialized) {
      await _pool.close();
      _isInitialized = false;
    }
  }
}
```

### Шаг 2: Инициализировать пул при старте и использовать его в Middleware

Нужно найти главный файл вашего сервера (вероятно, `bin/server.dart` или подобный) и инициализировать `DatabaseSource` там. Если такого файла нет, можно использовать кастомный `main.dart` для запуска.

Предположим, у вас есть `bin/server.dart`.

```dart
// bin/server.dart (или ваш главный файл)
import 'dart:io';
import 'package:back_garson/data/sources/database.dart';
import 'package:dart_frog/dart_frog.dart';

Future<HttpServer> run(Handler handler, InternetAddress ip, int port) {
  // Инициализируем пул соединений ОДИН РАЗ при старте сервера
  DatabaseSource.instance.initialize();
  
  return serve(handler, ip, port);
}
```

Теперь измените `routes/_middleware.dart`, чтобы он использовал **единственный** экземпляр пула.

```dart
// routes/_middleware.dart

// ... импорты ...
import 'package:back_garson/data/sources/database.dart';
import 'package:dart_frog/dart_frog.dart';
import 'package:postgres/postgres.dart'; // <-- Добавить импорт

// Провайдер для пула соединений
final _dbProvider = provider<Pool>((_) => DatabaseSource.instance.pool);

Handler middleware(Handler handler) {
  // Используем _dbProvider для внедрения зависимости
  return handler.use(_dbProvider).use(requestLogger());
  
  // Всю остальную логику по созданию сервисов нужно перенести
  // в отдельные провайдеры или непосредственно в маршруты,
  // где они нужны, чтобы не создавать их все на каждый запрос.
}
```

**Важное замечание:** В предложенном выше решении я также убрал создание всех сервисов из `middleware`. Создавать все сервисы на каждый запрос — это тоже неэффективно. Их следует либо также делать синглтонами (если у них нет состояния), либо создавать по мере необходимости в конкретных маршрутах, которые их используют.

Для доступа к пулу в репозиториях, их конструкторы должны будут принимать `Pool` вместо `DatabaseSource`.

Пример для `TableRepositoryImpl`:
```dart
// lib/data/repositories/table_repository_impl.dart
import 'package:postgres/postgres.dart';

class TableRepositoryImpl implements TableRepository {
  TableRepositoryImpl(this.pool); // <-- Принимаем Pool

  final Pool pool; // <-- Храним Pool

  @override
  Future<void> someQuery() async {
    // Используем пул для выполнения запроса
    await pool.execute(...);
  }
}
```

Это изменение кардинально решит проблему "Too many connections" и значительно улучшит производительность вашего приложения.
