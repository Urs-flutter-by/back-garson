# Сводка рефакторинга (Часть 6): Переход на bcrypt

**Дата:** 07.10.2025

## 🎯 Цель
Заменить устаревший и небезопасный алгоритм хэширования паролей **MD5** на современный и надежный **bcrypt**.

## 📝 Что сделано

### 1. Анализ проблемы
- В коде (`WaiterRepositoryImpl`) было обнаружено использование `md5` для хэширования паролей официантов.
- MD5 является небезопасным для хранения паролей из-за его высокой скорости вычисления и уязвимости к атакам (brute-force, радужные таблицы).

### 2. Внедрение bcrypt
- В проект была добавлена зависимость `dbcrypt` для работы с алгоритмом bcrypt.

### 3. Реализация "Ленивой миграции" (Lazy Migration)
Чтобы не сбрасывать пароли существующим пользователям, была реализована стратегия плавного обновления хэшей:
- **Логика входа (`signIn`) была полностью переработана.**
- При попытке входа система сначала получает хэш пользователя из базы данных.
- **Определяется формат хэша:**
    - Если это новый **bcrypt**-хэш, проверка происходит с помощью `DBCrypt().checkpw()`.
    - Если это старый **MD5**-хэш, проверка происходит по старой логике.
- **Автоматическое обновление:** Если проверка по MD5 прошла успешно, это означает, что пользователь ввел правильный пароль. Система **немедленно** создает для этого пароля новый bcrypt-хэш и **обновляет его в базе данных**.

### 4. Исправление ошибок
- После первоначальной реализации были выявлены и исправлены ошибки в использовании библиотеки `dbcrypt` (неправильные имена методов `check` и `hashpw`).
- Код был проверен с помощью `dart analyze` для гарантии качества.

## ✅ Результат
- Система аутентификации теперь использует **безопасный, отраслевой стандарт** для хранения паролей.
- Процесс миграции **полностью прозрачен** для пользователей и не требует от них никаких действий.
- Старые, небезопасные хэши будут постепенно вытесняться из базы данных по мере активности пользователей.
