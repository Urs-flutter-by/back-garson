# Сводка рефакторинга (Часть 2): Исправление предупреждений анализатора

Этот документ описывает изменения, внесенные для устранения предупреждений и замечаний статического анализатора (`dart analyze`).

## 🎯 Основная цель
Повысить качество и надежность кодовой базы путем исправления ошибок, предупреждений и важных стилистических замечаний, выявленных анализатором.

## 📝 Список изменений

### 1. Добавлена зависимость `crypto`
- **Файл:** `pubspec.yaml`
- **Что сделано:**
    - Добавлена зависимость `crypto: ^3.0.3`, необходимая для хэширования паролей.
    - Отсортированы все зависимости в алфавитном порядке в соответствии с рекомендациями.
- **Результат:** Устранена ошибка `depend_on_referenced_packages` и улучшен порядок в `pubspec.yaml`.

### 2. Исправлена типизация `Pool`
- **Файлы:** Все репозитории, `_middleware.dart` и все файлы маршрутов.
- **Что сделано:** Все вхождения `Pool` были заменены на `Pool<void>`.
- **Результат:** Устранены все предупреждения `strict_raw_type`, что делает код более строгим и типобезопасным.

### 3. Исправлены модели данных
- **Файлы:** `lib/data/models/shift_model.dart`, `lib/data/models/table_model.dart`.
- **Что сделано:**
    - Убраны некорректные аннотации `@override` у методов `toJson`.
    - В `shift_model.dart` исправлен оператор `?.` на `.` для non-nullable поля.
    - Добавлена аннотация `@override` для поля `halls` в `shift_model.dart`.
- **Результат:** Устранены предупреждения `override_on_non_overriding_member` и `invalid_null_aware_operator`.

### 4. Улучшена типобезопасность в маршрутах
- **Файлы:** Все файлы маршрутов в `routes/app/` и `routes/web/`.
- **Что сделано:** Добавлено явное приведение типов `as Map<String, dynamic>` при парсинге JSON-тела запроса.
- **Результат:** Устранены предупреждения `avoid_dynamic_calls`, что снижает вероятность ошибок во время выполнения.

---

После этих изменений `dart analyze` не сообщает об ошибках или предупреждениях (`0 errors, 0 warnings`). Оставшиеся информационные сообщения касаются в основном стиля кода и документации.
