# Сводка рефакторинга (Часть 4): Обновление зависимостей и исправление SSL

Этот документ описывает изменения, внесенные для обновления зависимостей проекта и устранения ошибки подключения к базе данных.

## 🎯 Основная цель
1.  Обновить все пакеты до последних стабильных мажорных версий.
2.  Устранить критическую ошибку `Server does not support SSL`, возникавшую после предыдущих рефакторингов.

## 📝 Список изменений

### 1. Обновление зависимостей
- **Команда:** `dart pub upgrade --major-versions`.
- **Что сделано:** Все зависимости в `pubspec.yaml` были обновлены. Ключевые обновления включают:
    - `very_good_analysis` с `v5.1.0` до `v10.0.0`.
    - `postgres` до `v3.5.8`.
    - Множество других пакетов, связанных со сборкой и разработкой.
- **Результат:** Проект теперь использует актуальные версии пакетов, что повышает стабильность и безопасность.

### 2. Исправление ошибки подключения (SSL)
- **Файл:** `lib/data/sources/database.dart`
- **Проблема:** После обновления пакет `postgres` стал по умолчанию требовать SSL-соединение, которое не поддерживалось локальным сервером БД в Docker, что приводило к ошибке при запуске.
- **Что сделано:**
    - Метод инициализации `DatabaseSource` был переписан для использования конструктора `Pool.withUrl`.
    - Вместо `Endpoint` и `PoolSettings` теперь используется единая строка подключения (URL).
    - В URL были добавлены параметры `sslmode=disable` и `max_connection_count=10` для корректной конфигурации пула соединений без SSL.
- **Результат:** Ошибка подключения полностью устранена. Приложение успешно запускается и подключается к базе данных.
