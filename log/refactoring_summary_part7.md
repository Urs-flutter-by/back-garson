# Сводка рефакторинга (Часть 7): Внедрение системы логирования

**Дата:** 07.10.2025

## 🎯 Цель
Заменить все вызовы `print` на структурированную систему логирования с использованием пакета `logging`.

## 📝 Что сделано

### 1. Выбор и добавление библиотеки
- Проведен анализ доступных библиотек для логирования на `pub.dev`.
- Выбран официальный пакет `logging` за его гибкость и масштабируемость, особенно для бэкенд-приложений.
- Пакет `logging` добавлен в `pubspec.yaml`.

### 2. Централизованная настройка логгера
- В файле `routes/_middleware.dart` настроен корневой логгер для вывода сообщений в консоль.
- Конфигурация включает:
    - Включение иерархического логирования (`hierarchicalLoggingEnabled = true`).
    - Установку уровня логирования (`Logger.root.level = Level.ALL`).
    - Создание слушателя (`Logger.root.onRecord.listen`), который форматирует и выводит логи в консоль, включая время, уровень, имя логгера, сообщение, а также ошибки и стектрейсы.
- Обеспечена однократная инициализация логгера при первом запросе к приложению.

### 3. Замена `print` на вызовы логгера
- Все найденные закомментированные вызовы `print` в файлах `lib/` и `routes/` были заменены на соответствующие вызовы логгера:
    - `_log.fine()` для детальной отладочной информации.
    - `_log.info()` для общих информационных сообщений.
    - `_log.warning()` для предупреждений.
    - `_log.severe()` для ошибок в блоках `catch`.
- В каждый файл, где используется логгер, добавлен импорт `package:logging/logging.dart` и статическая переменная `final _log = Logger('ClassName');`.

### 4. Исправление предупреждений анализатора
- Устранены все критические ошибки и большинство предупреждений анализатора, возникших в процессе рефакторинга (например, `undefined_method`, `not_enough_positional_arguments`, `lines_longer_than_80_chars`, `avoid_print`, `unused_element`, `unnecessary_late`).
- Для намеренных использований `print` в слушателе логгера добавлены комментарии `// ignore: avoid_print`.

## ✅ Результат
- Приложение теперь использует структурированную систему логирования, что значительно улучшает отладку и мониторинг.
- Логи выводятся в консоль в читаемом формате.
- Код стал чище и соответствует лучшим практикам.
